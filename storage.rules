rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null && (request.auth.uid in ['mzzUSsOeFSMHZ6pVoU9m0Xmy75q2', 'xjKksZQ6pcT1bCfiIlOgnVhuuw73']);
    }
  }
}
